-- =============================================================================
-- LazyVim Bootstrap — ~/.config/nvim/init.lua
--
-- This is the entry point for Neovim. It loads the lazy.nvim package manager
-- and the LazyVim starter configuration. No dynamic git cloning required —
-- lazy.nvim bootstraps itself on first launch.
-- =============================================================================

require("config.lazy")
